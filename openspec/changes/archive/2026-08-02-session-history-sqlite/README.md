# session-history-sqlite

Session history moves from append-only JSONL to an indexed SQLite store; fills the cache-creation, latency, serving-provider, and translated-request/raw-response capture gaps.
